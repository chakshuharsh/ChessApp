We need to create a state holder class for this app like a viewmodel to handle configuration changes
we also need to follow the architectural best practices
don't pass the viewmodel instances to composable which only need some data/ methods of viewmodel



Currently:-
How can we structure the code more effectively
we need to solve the king safety bug
and to implement the moves feature
we have a composable which takes a list and we would call that twice one above the board UI and then below it again for each player and pass the respective list to each composable
we want to know how many recomposition are happening for each composable function
app became too slow and also add following-
1. * sign for capture -> Facing a bug
2. + sign for check
3. # sign for checkmate


Estimate the load on main thread
Add animation to piece movements



LATEST:-
finding position of a game and converting it into the format of initialPosition and then setting the board from it
Implementing Fisher Random
Implementing time
Implementing Castling


What are the first requirements
Handling checks and threats correctly
Game Should stop after checkmate or any other way of the game ends (Draw and resigns)


how an online chess game can be implemented I mean what to use whenever a player moves
when a player is online and another as well then we have to setup a data stream between two players
may be when a successful connection is being made we can just pass the gameId, move to server and server will pass the latest move to other player
where does the calculation happens on the server or on the phone probably on the phone




Handling checks and restricting to saving moves
find set of moves
and disable other pieces


ok so when there is a threat on the king we can have many threatening pieces
 -> we should also restrict moves which exposes kings to threats
 There are 2 cases for threats
 1. if the threatening pieces is one -> attack, block, displacement
 2. if it is more than one -> displacement
 The king always has to be moved if it's in double check,
 because it's impossible to block 2 lines of attack in one move

 if the T.P == 1 then we will follow the first strategy
which is finding blocking piece, attacking piece and finding king safety moves
let's deep in each
        Attack:
            finding pieces from defensive side which has moves and contains attacking piece
                pieces -> list of defensive pieces
            |<-  val defensePiece:List<Pieces> = pieces.filter{piece ->
            |     piece.color != T.P.color && piece.getAvailableMoves(piece,pieces).contains(T.P?.position)
            |     }
            |     from this we can find the pieces which can save the king
            |     and if king is under threat then player can select pieces which are from special package only -> "soldiers"
            |->     we will place the pieces from above calculation into this package and also from other calculation like pinning/ blocking
                    when user selects pieces from "Attacking soldiers" then moves would be the position of T.P that's it


we have bord class we will make it a data layer class and then we will have a viewmodel which imitates all functions of Board clas and then we will use the viewmodel in UI layer and with other functions as well


When king is under double threat only the king's cell should be able to click
so when  the threatening pieces contain more than  piece we would mark the respective
isKingUnderThreat = true and numberOfThreats to 1
(which means more than one threat and 0 means less than 2 threat(single threat))
composable would observe this and when they see that number of threats are more than one we should make all the composable disabled for clicking except the one which contains the king
ok so the select function should be involved when a piece is clicked

I have a list of threatening piece
and this is in this file so tell me how can I use it in my board class in various


Explain the problem the threat is there but the handing is not properly



Castling
Sound
Timer
Game Ending -> Draw(Agreed + forced stalemate + insufficient material + repetition), Checkmate
Different Timer
Chess 960




Current Refactoring
how to pass function as parameter
optimize the board cell composable by passing only required stuff

